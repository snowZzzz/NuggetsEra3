<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.nuggetsera"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/login_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity"></activity>

        <activity android:name=".ui.StartActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>
        <activity android:name=".ui.LoginActivity"></activity>
        <activity android:name=".ui.RegisteredActivity"></activity>
        <activity android:name=".ui.RegisteredNextActivity"></activity>
        <activity android:name=".ui.ChangePasswordActivity"></activity>
        <activity android:name=".ui.ChangePasswordTwoActivity"></activity>
        <activity android:name=".ui.MyActivity"></activity>

    </application>

</manifest>